<div class="image-view">
  <div class="container">
  <div style="margin: 50px;">
  <% @images.each do |image| %>
    <%= image_tag image.photo(:thumb), id: "#{image.id}", style: "margin-left:20px;", size: "200x300" %>
  <% end %>
  </div>
  </div>
</div>